<UserControl x:Class="PassManager.View.PasswordGen"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Controls="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
             xmlns:bh="clr-namespace:PassManager.Behavior"
             xmlns:c="clr-namespace:PassManager.Converter"
             xmlns:v="clr-namespace:PassManager.View"
             xmlns:vm="clr-namespace:PassManager.ViewModel"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:local="clr-namespace:PassManager.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <UserControl.Resources>
        <c:MultiBindingConverter x:Key="MultiBindingConverter" />
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="col0" Width="0.2*" />
            <ColumnDefinition x:Name="col1" Width="0.7*" />
            <ColumnDefinition x:Name="col2" Width="0.1*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Column="0" Grid.Row="0" Text="タイトル" VerticalAlignment="Center" />
        <TextBox   Grid.Column="1" Grid.Row="0" Text="{Binding Title}" />
        <TextBlock Grid.Column="0" Grid.Row="1" Text="ユーザ名" VerticalAlignment="Center" />
        <TextBox   Grid.Column="1" Grid.Row="1" Text="{Binding UserName}" />

        <TextBlock Grid.Column="0" Grid.Row="2" Text="プロファイル" VerticalAlignment="Center" />
        <ComboBox  Grid.Column="1" Grid.Row="2" Margin="8"/>
        <TextBlock Grid.Column="0" Grid.Row="3" Text="パスワードタイプ" VerticalAlignment="Center" />
        <WrapPanel Grid.Column="1" Grid.Row="3">
            <CheckBox Content="数字" IsChecked="{Binding DecimalFlg}" />
            <CheckBox Content="小文字アルファベット" IsChecked="{Binding a_AlpFlg}" />
            <CheckBox Content="大文字アルファベット" IsChecked="{Binding A_AlpFlg}" />
            <CheckBox Content="記号" IsChecked="{Binding SymbolFlg}" />
        </WrapPanel>
        <TextBlock Grid.Column="0" Grid.Row="4" Text="文字列長" VerticalAlignment="Center" />
        <TextBox   Grid.Column="1" Grid.Row="4" Text="{Binding PassLength}" />
        <Button    Grid.Column="2" Grid.Row="4" Content="生成" Command="{Binding CommandGenPassword}" />

        <TextBlock Grid.Column="0" Grid.Row="5" Text="Password" VerticalAlignment="Center" />
        <TextBox   Grid.Column="1" Grid.Row="5" Text="{Binding PasswordString}" />
        <ToggleButton    Grid.Column="2" Grid.Row="5" Content="表示" IsChecked="{Binding PasswordViewFlg}"/>
        <ItemsControl Grid.Column="0" Grid.ColumnSpan="3" Grid.Row="6" ItemsSource="{Binding Memos}" AlternationCount="{Binding MemosCnt}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.2*" />
                            <ColumnDefinition Width="0.7*" />
                            <ColumnDefinition Width="0.1*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="memo" />
                        <TextBox Grid.Column="1" Text="{Binding Path=.}" AcceptsReturn="True">
                            <i:Interaction.Triggers>
                                <i:EventTrigger EventName="TextChanged">
                                    <i:InvokeCommandAction Command="{Binding RelativeSource={RelativeSource FindAncestor,
                                                           AncestorType={x:Type UserControl}, AncestorLevel=1}, Path=DataContext.CommandTextChanged}">
                                        <i:InvokeCommandAction.CommandParameter>
                                            <MultiBinding Converter="{StaticResource MultiBindingConverter}">
                                                <Binding RelativeSource="{RelativeSource TemplatedParent}" Path="(ItemsControl.AlternationIndex)" />
                                                <Binding RelativeSource="{RelativeSource FindAncestor,
                                                         AncestorType={x:Type TextBox}, AncestorLevel=1}" Path="Text" />
                                            </MultiBinding>
                                        </i:InvokeCommandAction.CommandParameter>
                                    </i:InvokeCommandAction>
                                </i:EventTrigger>
                            </i:Interaction.Triggers>
                        </TextBox>
                        <Button Grid.Column="2" Content="追加" Command="{Binding RelativeSource={RelativeSource FindAncestor,
                                AncestorType={x:Type UserControl}, AncestorLevel=1}, Path=DataContext.CommandMemoAdd}" />
                    </Grid>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
        
        <DockPanel Grid.Column="0" Grid.Row="8" Grid.ColumnSpan="2" HorizontalAlignment="Right" >
            <Button Content="キャンセル" HorizontalAlignment="Center" Command="{Binding DataContext.GenCanncelCommand, 
                    RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type v:MainWindow}}}" 
                    Width="100" />
            <Button Content="作成" HorizontalAlignment="Right" MinWidth="100" Command="{Binding CommandCreate}"
                    CommandParameter="{Binding RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type UserControl}, AncestorLevel=1}}"/>
        </DockPanel>
    </Grid>
</UserControl>
